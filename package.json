{
  "name": "auto-responder",
  "version": "0.0.1",
  "description": "Plugin for url-replace. It allows to return local files instead of calling server",
  "main": "auto-responder.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "https://github.com/zboro/auto-responder.git",
  "author": "zboron.lukas@gmail.com",
  "license": "ISC"
}
